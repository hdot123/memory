stages:
  - test
  - sync

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.pip-cache"

cache:
  paths:
    - .pip-cache/

test:
  stage: test
  variables:
    PYTHONPATH: "."
  before_script:
    - python3 -m venv /tmp/ci-venv
    - source /tmp/ci-venv/bin/activate
    - pip install --upgrade pip pytest ruff
  script:
    - source /tmp/ci-venv/bin/activate
    - ruff check .
    - python -m pytest -q tests
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push"'

sync-to-github:
  stage: sync
  needs: [test]
  rules:
    - if: '$CI_COMMIT_BRANCH == "main" && $CI_PIPELINE_SOURCE == "push"'
  before_script:
    - git config --global user.name "gitlab-ci"
    - git config --global user.email "ci@node-15.tail5e888.ts.net"
  script:
    - GH_REMOTE="https://$(printf 'x-access-token:%s' "$GITHUB_TOKEN")@github.com/hdot123/memory.git"
    - git remote add github "$GH_REMOTE" || true
    - git push github main --force
