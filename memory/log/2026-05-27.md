# 2026-05-27 Daily Facts

## GitHub Sync Fix (会话 ID: b7155a00-deb4-454d-b6ca-78c9702241ad)

### 1. 彻底解决 GitHub 同步失败
- **根因**：3 层问题叠加
  - `GIT_DEPTH: 20` 浅克隆 → 添加 `GIT_DEPTH: "0"`
  - PAT 缺少 `workflow` scope → 新 token 包含 `repo` + `workflow`
  - CI 变量 `GITHUB_REPO_OWNER/NAME` 缺失 → 添加配置

### 2. 共享 CI 模板体系
- 创建 `infra/ci-templates/github-sync.yml` v2（通用变量化 URL）
- 更新 `infra/gitlab-ci-standards/templates/full.yml`
- 更新 `memory-core/.gitlab-ci.yml` 改为 include 模板

### 3. README 中文重写
- `infra/ci-templates/README.md` → 中文
- `infra/gitlab-ci-standards/README.md` → 中文

### 4. 1Password 凭证整理
- 新 token 存入 `GitHub / hdot123 / Personal Access Token`
- 清理冗余字段，添加标签 `ci, github, gitlab, high-privilege`

### 5. 文档补充
- GitLab `CI_CD_RUNBOOK.md` 新增第八章节
- ShowDoc 同步更新（3 个故障排查 + 3 条新经验教训）

### 验证结果
- ✅ 所有 pipeline 通过（test、health-check、sync-to-github）
- ✅ GitHub 镜像 SHA 与 GitLab 完全一致
- ✅ 11/11 验证断言全部通过

## Memory Root Discovery Bug Fix (会话 ID: b7155a00-deb4-454d-b6ca-78c9702241ad)
- **根因**：`discover_project_root()` 返回最内层 `memory/system/` 标记而非最外层，导致 gateway 解析到 `memory_core/` 而非消费项目根目录
- **影响**：所有写入路径计算为 `memory/log/` 而非 `{workspace_root}/memory/log/`
- **修复**：改为追踪 `outermost` 变量，继续向上遍历直至 git sentinel 或最大深度，返回最外层匹配
- **MR #24**：已合并（算法修复 + `test_outermost_wins`）
- **MR #26**：已合并（补充 `test_nested_consumer_case` + `test_discover_roots_nested`）
- **测试**：14/14 通过
- **规范修复**：`AGENTS.md` 添加中文 commit 铁律；`python-worker/SKILL.md` 添加中文 commit 要求
- **同类问题排查**：未发现其他子目录误导问题

## memory_core/memory/ → memory/ 目录统一迁移 (MR #29)

### 背景
`memory_core/memory/` 下的 docs、kb、system 等数据目录与 Python 包代码 `memory_core/` 混杂，导致路径混乱、gateway 解析复杂、artifacts 写入旧路径等问题。

### 迁移内容
| 原路径 | 新路径 | 说明 |
|--------|--------|------|
| `memory_core/memory/docs/` | `memory/docs/` | 设计文档、RFC、运维手册等 |
| `memory_core/memory/kb/` | `memory/kb/` | 知识库（全局规则、决策、教训） |
| `memory_core/memory/system/` | `memory/system/` | 系统状态（ownership.toml 等） |
| `memory_core/memory/inbox.md` | `memory/inbox.md` | 收件箱 |
| `memory_core/project-map/` | `project-map/` | 项目地图（repo 根目录） |
| `memory_core/artifacts/` (535MB) | `memory/artifacts/` | 运行时 hook 产物 |
| `memory_core/INDEX.md` | `INDEX.md` | 工作区根索引（新建，含治理标记） |

### 代码修改（25+ 文件）
- **9 个源码文件**：`memory_hook_gateway.py`、`cmux_hook_state.py`、`memory_hook_impls.py`、`daily_session_summary.py`、`check_boundary.py`、`init_project_memory.py` 等 — 更新 artifacts 路径从 `artifacts/` 到 `memory/artifacts/`，修复 kb/docs 路径引用
- **16 个测试文件**：同步更新断言中的路径
- **pretooluse_guard.py**：新增 `gitlab_api_push.py` 白名单（解决 `memory/` 路径被拦截问题）
- **ownership.toml**：修正 `project_map` 域路径、新增 `memory/artifacts/` 和 `memory/log/` 域

### 清理
- 删除 `memory_core/` 下残留的 `INDEX.md`、`NOW.md`、`projects/AEdu/INDEX.md`
- 删除 `memory_core/memory/`（仅剩 `schema-audit.log`）
- 删除 `memory_core/artifacts/`（已迁移到 `memory/artifacts/`）
- 删除 10 个已弃用的 workbot adapter 测试文件
- `memory_core/` 现在只剩 54 个 `.py` 文件（纯净 Python 包）

### 文档更新
- 5 个文档旧路径引用已修复（`01-architecture.md`、`08-data-pipeline.md` 等）
- `.gitignore` 更新 `memory/artifacts/`

### 验证
- 476+ tests passed, 0 failures
- CI Pipeline #317 success（ruff lint + pytest）
- MR #29 合并 → Pipeline #318 running → sync-to-github 触发

### 关键教训
- **迁移前先确认目标位置符合架构规范**：artifacts 应在 `memory/` 内，不散落 repo 根目录
- **pretooluse_guard 兜底规则会拦截含 `memory/` 的未知命令**：需为 `gitlab_api_push.py` 等 API 推送工具添加白名单
- **ownership.toml 路径需与实际目录同步**：迁移后必须检查域和资源声明
